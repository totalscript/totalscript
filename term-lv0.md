## Term language, level 0

Main ideas:

1. Pi : `(x : T) -> y` or `{x : T} -> y`.
2. Inductives : `inductive t : T { ctor : ... }`.
3. Applications : `x y` for explicit Pi, `x @y` for implicit Pi.
4. Records are unrolled into inductives.
5. Recursive terms must be introduced explicitly.

